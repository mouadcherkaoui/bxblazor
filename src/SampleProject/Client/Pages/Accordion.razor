@page "/components/bx-accordion"

<h3>Accordion</h3>
<div class="bx--grid">
    <div class="bx--row">
        <div class="bx--offset-lg-2 bx--col-lg-10">
            <BxAccordion Items="Items" />
        </div>
    </div>
</div>
@code {
    public IEnumerable<AccordionItem> Items { get; set; }
        = new List<AccordionItem>
        {
            new AccordionItem
            {
                Title = "first pane",
                Content = (tree) => tree.AddMarkupContent(0, "<p>a sample content with some tags in it, <strong>First item!</strong></p>")
            },
            new AccordionItem
            {
                Title = "second pane",
                Content = (tree) => tree.AddMarkupContent(0, "<p>a sample content with some tags in it, <strong>First item!</strong></p>")
            },
            new AccordionItem
            {
                Title = "third pane",
                Content = (tree) => tree.AddMarkupContent(0, "<p>a sample content with some tags in it, <strong>First item!</strong></p>")
            }
        };
}
